Given five numbers as input. Calculate and print the average of the numbers.

Solution

let number = [45, -12, 0, 3, -15];
let sum = 0;
for(let i = 0; i < number.length; i++){
  sum += number[i];
}
console.log(sum / number.length)

Մյուսը՝

let number = [7, 52, -23, 9, -81];
let sum = 0;
for(let i = 0; i < number.length; i++){
  sum += number[i];
}
console.log(sum / number.length)